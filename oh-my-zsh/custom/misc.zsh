# alias to activate a virtualenv environment. most of my envs are named workingEnv
alias ue="source workingEnv/bin/activate"

# to fix MySQL-Python dynamic library (client) issues
export DYLD_LIBRARY_PATH=$DYLD_LIBRARY_PATH:/usr/local/mysql/lib/

